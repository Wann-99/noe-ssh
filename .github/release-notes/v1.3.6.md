# Noe-SSH v1.3.6

## 修复

- **Linux `.deb` / 桌面端「Server failed to start」**：安装目录 `/opt` 不可写，内嵌服务无法在其中创建数据库；改为写入用户数据目录（Linux：`~/.config/Noe-SSH/data`，并由 `NOE_SSH_DATA_DIR` 明确指定）
- 启动失败时错误框会附带内嵌服务日志，便于排查

## 说明

- 请用本版本 `.deb` 覆盖安装后再从菜单启动
- Docker 镜像：`ghcr.io/wann-99/noe-ssh:1.3.6` / `:latest`
