# Noe-SSH v1.3.8

## 修复

- 修复 Linux `.deb` 打包失败：`afterInstall` 脚本中的 `${size}` 被 electron-builder 误解析为宏

## 说明

- 功能同 v1.3.7（浮动文件编辑、收纳箱、深色高亮、真正退出、多尺寸图标）
- Docker 镜像：`ghcr.io/wann-99/noe-ssh:1.3.8` / `:latest`
