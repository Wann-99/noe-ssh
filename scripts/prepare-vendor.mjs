/**
 * Legacy vendor copy for the pre-React public UI.
 * The Vite client bundles xterm; this script is now a no-op kept for script compatibility.
 */
console.log('prepare-vendor: skipped (client bundles @xterm via Vite)');
